# Calibration diagnostic data

ArrowLab developer diagnostic CSV files are written here by tools/capture_creep.py.

Raw datasets may be committed when they form part of a documented calibration/creep investigation. Do not overwrite original acquisition files; derived/cleaned datasets should use a new filename.
